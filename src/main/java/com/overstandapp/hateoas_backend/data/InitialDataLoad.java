package com.overstandapp.hateoas_backend.data;
/*

import com.overstandapp.hateoas_backend.domain.Capability;
import com.overstandapp.hateoas_backend.domain.User;
import com.overstandapp.hateoas_backend.repositories.CapabilityRepository;
import com.overstandapp.hateoas_backend.repositories.UserRepository;
import org.springframework.boot.CommandLineRunner;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

*/
//This class is used to load the sample data each time the server starts/restarts
 
//@Configuration
public class InitialDataLoad {



/*
    @Bean
    CommandLineRunner LoadDB (CapabilityRepository capabilityRepository) {
        return args -> { //lambda expression
            capabilityRepository.save(new Capability("TypeScript", 100, 100));
            capabilityRepository.save(new Capability("Php", 100, 100));
            capabilityRepository.save(new Capability("Golang", 100, 100));
        };
    }


    @Bean
    CommandLineRunner LoadMoreData (UserRepository userRepository) {
        return args -> {
            userRepository.save(new User("Kello", "Ibrahim", "08083862828", "kello@gmail.com"));
            userRepository.save(new User("Jojo", "Ogbhemhe", "234980790","jojo@jojobenard.com"));
        };
    }
    */

}
